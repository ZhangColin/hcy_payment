spring:
  # 本地开发数据源配置
  datasource:
    url: jdbc:postgresql://localhost:5432/aieducenter-payment
    username: aiedu
    password: dev123

  # Redis 配置
  data:
    redis:
      host: localhost
      port: 6379

# 开发环境：显示 SQL
spring.jpa.show-sql: true
